class Solution {
public:
    bool isSameAfterReversals(int num) {
    //    int digit = 0;
    //    while(num )
        if(num==0) return true;
        if(num%10 ==0) return false;     
        return true;

    }
};